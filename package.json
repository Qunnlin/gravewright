{
  "name": "gravewright",
  "private": true,
  "version": "1.5.2",
  "description": "GRAVEWRIGHT — an incremental necromancy roguelike. Your heroes die. That's the point.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@fontsource/cinzel": "^5.1.0",
    "@fontsource/im-fell-english": "^5.2.6",
    "@fontsource/jetbrains-mono": "^5.1.0",
    "zzfx": "^1.2.0"
  }
}
